# Archive Index - 2026-08

| Feature | Archived | Match Rate | Documents |
|---------|----------|-----------|-----------|
| swing-mapped-position-adoption | 2026-08-26 | 97% | plan, design, analysis, report |

> **후속 변경 2026-09-17** — 배치 진입 가드가 DB값 대신 증권사 실보유로 편입하도록 변경(analysis G3 해소).
> 아카이브 시점 Low 평가를 Med로 재조정했다: 발생 확률은 낮으나 자가 회복이 불가능한 결함이었다.
> 상세는 design 문서의 「후속 변경 상세」 참고. 전체 테스트 70 passed.

## ⚠️ 후속 항목 (배포 전 권장)
- **swing-mapped-position-adoption**: 정적 분석 97%(Gap 2건 즉시 수정, 잔여는 Low/Info). **KIS 실계좌 검증 미수행** — 실보유 조회 응답 형식(국내 `get_stock_balance` / 해외 `get_us_holdings`의 `pchs_avg_pric`·`prpr` 실제값), 편입 후 실제 매도 체결, 활성화 시 3년치 적재 소요시간을 실호출로 확인해야 한다. 검증 범위는 스텁 기반 사이클 실행(60 테스트)까지다.
- 부수 수정된 푸시 알림 3건(전량매도 미발송, 수량/단가 0 표기, 1차 익절 수량 오표기)은 실기기 푸시 수신 확인이 필요하다.
- 프론트엔드 대응 필요: 활성화 응답의 `DATA_STATUS`(`READY`/`PREPARING`/`SKIP`) 처리 — `PREPARING`은 "데이터 준비중, 곧 매매 시작" 표시.
- 배치 가드가 도달하지 못하는 두 경로(지표 캐시 없음 `auto_swing_batch.py:188`, 미확인 주문 미해결 `:372`)는 편입이 지연된다. 두 경로 모두 매수를 실행하지 않으므로 포지션 유실은 없다.
- **(2026-09-17 추가)** 배치 가드도 실보유 조회 응답에 의존하게 되어, 위 "KIS 실계좌 검증 미수행" 항목의 실측 범위가 활성화 경로에서 **배치 경로까지** 넓어졌다.
